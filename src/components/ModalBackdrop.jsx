const ModalBackdrop = () => {
    return (
        <div
            id="modalBackdrop"
            className="absolute inset-0 bg-gray-500 bg-opacity-75 transition-opacity"
        />
    )
}

export default ModalBackdrop
